services:
  mcserver:
    build: mcs
    container_name: MinecraftServer_v26.1
    volumes:
      - world_data:/work
      - ./volume:/work/world
    ports:
      - 25565:25565
    tty: true
    stdin_open: true
    restart: on-failure

volumes:
  world_data:
